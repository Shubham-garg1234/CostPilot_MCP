{
  "mcpServers": {
    "costpilot": {
      "type": "stdio",
      "command": "node",
      "args": [
        "${workspaceFolder}/costpilot-mcp.mjs"
      ],
      "envFile": "${workspaceFolder}/.env"
    }
  }
}
